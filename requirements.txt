fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
pydantic==2.7.4
python-multipart==0.0.9
passlib==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
sentence-transformers==2.7.0
pymupdf==1.24.5
python-docx==1.1.2
numpy==1.26.4

# NOTE: torch is intentionally NOT pinned here. Install it separately using
# the CPU-only index (see Dockerfile / README) — the default PyPI wheel
# includes CUDA/GPU support and is far too large and memory-heavy for
# Render's free 512MB instance.
